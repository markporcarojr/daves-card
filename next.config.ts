import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["unsplash.com", "images.unsplash.com"], // Add 'unsplash.com' here
  },
  // Other config options here
};

export default nextConfig;
